services:
  app:
    build:
      context: ./disq
      args:
        - DATABASE_URL=${DATABASE_URL}
    ports:
      - "3001:3001"
    env_file:
      - ./disq/.env                                                                                                                                                                      